os.loadAPI("module")
module.require('ld')

ldClientSideId = "CLIENT-SIDE-ID"
ldFlagKey = "FLAG-KEY"

ld.init(ldClientSideId)

while true do
	sleep(0)
    value = ld.variation(ldFlagKey, false)
    for key,side in pairs(redstone.getSides()) do
        redstone.setOutput(side, value)
    end
end
